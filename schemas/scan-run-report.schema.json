{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saferskills.ai/schemas/scan-run-report.schema.json",
  "title": "ScanRunReport",
  "description": "Full report for one repo scan (a `scan_runs` row): the consolidated repo score, the by-kind tally, and every capability discovered in the repo with its own independent score + findings. Backs GET /api/v1/scans/runs/<run_id> and the `/scans/<id>` page. The wire shape is report-flavoured (nested `capabilities`); the `scan_runs` table columns are the scalar fields here plus the x-postgresql-extra-columns below.",
  "x-postgresql-classname": "ScanRun",
  "x-postgresql-tablename": "scan_runs",
  "x-postgresql-indexes": [
    { "name": "idx_scan_runs_expires_at", "columns": ["expires_at"], "where": "visibility = 'unlisted'" }
  ],
  "x-postgresql-extra-columns": [
    { "name": "idempotency_key", "type": "varchar", "length": 64, "nullable": false, "unique": true },
    { "name": "file_count", "type": "integer", "nullable": false, "default": "0" },
    { "name": "content_hash_sha256", "type": "varchar", "length": 64, "nullable": true },
    { "name": "original_filename", "type": "varchar", "length": 255, "nullable": true },
    { "name": "share_token", "type": "varchar", "length": 64, "nullable": true, "unique": true },
    { "name": "created_at", "type": "timestamp", "nullable": false, "default": "now()" },
    { "name": "updated_at", "type": "timestamp", "nullable": false, "default": "now()" }
  ],
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "repoAggregateScore",
    "repoTier",
    "kindTally",
    "capabilityCount",
    "capabilities",
    "scannedAt",
    "rubricVersion",
    "engineVersion",
    "latencyMs",
    "source",
    "status"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid",
      "x-primary-key": true,
      "x-postgresql-default": "gen_random_uuid()",
      "description": "Server-assigned scan_runs UUID. Keys the `/scans/<id>` page + the SSE channel."
    },
    "githubUrl": {
      "type": ["string", "null"],
      "format": "uri",
      "description": "Canonical GitHub repo URL scanned. Null for uploaded artifacts (`sourceKind = 'upload'`)."
    },
    "repoAggregateScore": {
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "x-postgresql-default": "0",
      "description": "Consolidated repo score — the rounded mean of the per-capability aggregate scores."
    },
    "repoTier": {
      "type": "string",
      "enum": ["green", "yellow", "orange", "red", "unscoped"],
      "x-postgresql-enum-type": "tier",
      "x-postgresql-default": "'unscoped'",
      "description": "5-tier badge for the consolidated repo score."
    },
    "kindTally": {
      "type": "object",
      "additionalProperties": {"type": "integer", "minimum": 0},
      "x-postgresql-default": "'{}'::jsonb",
      "description": "Count of discovered capabilities by kind, e.g. `{\"skill\": 3, \"hook\": 2, \"mcp_server\": 1}`."
    },
    "capabilityCount": {
      "type": "integer",
      "minimum": 1,
      "x-postgresql-default": "0",
      "description": "Number of capabilities discovered in the repo (always ≥1 — the whole-repo fallback)."
    },
    "capabilities": {
      "type": "array",
      "items": {"$ref": "#/$defs/capabilityRow"},
      "x-postgresql-skip": true,
      "description": "Each capability discovered in the repo, with its own independent score + findings."
    },
    "scannedAt": {
      "type": "string",
      "format": "date-time",
      "x-postgresql-default": "now()",
      "description": "When the repo scan completed (UTC ISO-8601)."
    },
    "rubricVersion": {
      "type": "string",
      "description": "Rubric git SHA active for this scan."
    },
    "engineVersion": {
      "type": "string",
      "description": "Scan-engine version for this scan."
    },
    "latencyMs": {
      "type": "integer",
      "minimum": 0,
      "x-postgresql-default": "0",
      "description": "Wall-clock latency of the repo scan in milliseconds."
    },
    "source": {
      "type": "string",
      "x-postgresql-enum-type": "scan_source",
      "description": "Scan trigger source (submission / ingestion / rescan_drift / rescan_appeal / rescan_rules)."
    },
    "status": {
      "type": "string",
      "enum": ["pending", "running", "completed", "failed"],
      "x-postgresql-enum-type": "scan_run_status",
      "x-postgresql-default": "'pending'",
      "description": "Run lifecycle state. A pending/running run renders the live progress board."
    },
    "refSha": {
      "type": ["string", "null"],
      "description": "Resolved HEAD SHA of the scanned ref. Null until the engine resolves it (and always null for uploads)."
    },
    "visibility": {
      "type": "string",
      "enum": ["public", "unlisted"],
      "default": "public",
      "x-postgresql-enum-type": "visibility",
      "x-postgresql-default": "'public'",
      "x-postgresql-nullable": false,
      "description": "Listing posture of the run. `unlisted` runs are reachable only via their capability URL and never appear in any public feed."
    },
    "sourceKind": {
      "type": "string",
      "enum": ["github", "upload"],
      "default": "github",
      "x-postgresql-enum-type": "source_kind",
      "x-postgresql-default": "'github'",
      "x-postgresql-nullable": false,
      "description": "Origin of the scanned bytes — `github` or `upload`."
    },
    "artifactSha256": {
      "type": ["string", "null"],
      "pattern": "^[a-f0-9]{64}$",
      "x-postgresql-skip": true,
      "description": "Content hash of the scanned artifact snapshot (`scan_runs.content_hash_sha256`). The durable fingerprint of an uploaded artifact. Null for legacy runs. (Persisted as the `content_hash_sha256` column — see x-postgresql-extra-columns.)"
    },
    "uploadedFilename": {
      "type": ["string", "null"],
      "maxLength": 255,
      "x-postgresql-skip": true,
      "description": "Original filename of the uploaded artifact (e.g. `SKILL.md`, `my-skill.zip`). Null for GitHub scans. (Persisted as the `original_filename` column — see x-postgresql-extra-columns.)"
    },
    "expiresAt": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Auto-expiry timestamp for an unlisted run (90 days). Null for public runs."
    },
    "shareUrl": {
      "type": ["string", "null"],
      "format": "uri",
      "x-postgresql-skip": true,
      "description": "The capability URL `/scans/r/<token>` — present only on the unlisted detail response served via the token route. NEVER present in any list payload."
    },
    "reportUrl": {
      "type": ["string", "null"],
      "format": "uri",
      "x-postgresql-skip": true,
      "description": "The canonical public report URL on the webapp (`<public_base_url>/scans/<run_id>` for a public run, or the `/scans/r/<token>` capability URL for an unlisted run). Built from the server's `public_base_url`, so a client (e.g. the CLI) need not know the webapp origin — which differs from the API origin in local dev. Null on legacy responses."
    }
  },
  "$defs": {
    "capabilityRow": {
      "type": "object",
      "additionalProperties": false,
      "description": "One discovered capability (a catalog item) within the repo scan.",
      "required": [
        "kind",
        "name",
        "aggregateScore",
        "tier",
        "scanId",
        "catalogSlug",
        "subScores",
        "findingsSummary",
        "findings"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["skill", "mcp_server", "hook", "plugin", "rules"],
          "description": "Capability taxonomy — drives the row's type glyph."
        },
        "name": {
          "type": "string",
          "description": "Capability display name (frontmatter / manifest name, or repo name for a whole-repo capability)."
        },
        "componentPath": {
          "type": ["string", "null"],
          "description": "Relative path of the scanned capability subtree (null / empty = whole-repo)."
        },
        "aggregateScore": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100,
          "description": "This capability's independent weighted aggregate score."
        },
        "tier": {
          "type": "string",
          "enum": ["green", "yellow", "orange", "red", "unscoped"],
          "description": "5-tier badge for this capability."
        },
        "scanId": {
          "type": "string",
          "format": "uuid",
          "description": "The `scans` row UUID for this capability."
        },
        "catalogSlug": {
          "type": "string",
          "description": "Catalog slug — links to `/items/<catalogSlug>`."
        },
        "subScores": {
          "type": "object",
          "additionalProperties": {"type": "integer"},
          "description": "5-axis sub-scores for this capability."
        },
        "findingsSummary": {
          "type": "object",
          "additionalProperties": false,
          "required": ["critical", "high", "medium", "low", "info", "total"],
          "properties": {
            "critical": {"type": "integer", "minimum": 0},
            "high": {"type": "integer", "minimum": 0},
            "medium": {"type": "integer", "minimum": 0},
            "low": {"type": "integer", "minimum": 0},
            "info": {"type": "integer", "minimum": 0},
            "total": {"type": "integer", "minimum": 0}
          },
          "description": "Per-severity finding counts for the collapsed row summary."
        },
        "findings": {
          "type": "array",
          "items": {"$ref": "#/$defs/finding"},
          "description": "This capability's findings (one per rule fire), shown when the row expands."
        }
      }
    },
    "finding": {
      "type": "object",
      "additionalProperties": false,
      "description": "Inline mirror of finding.schema.json. Keep in sync with the source schema.",
      "required": [
        "id",
        "ruleId",
        "severity",
        "subScore",
        "penalty",
        "statusAtScan",
        "filePath",
        "lineStart",
        "matchedContentSha256",
        "remediationLink",
        "rubricVersion"
      ],
      "properties": {
        "id": {"type": "string", "format": "uuid"},
        "ruleId": {
          "type": "string",
          "pattern": "^SS-(MCP|SKILL|RULES|HOOKS|PLUGIN)-[A-Z][A-Z0-9-]*-\\d{2}$"
        },
        "severity": {"type": "string", "enum": ["info", "low", "medium", "high", "critical"]},
        "subScore": {"type": "string", "enum": ["security", "supply_chain", "maintenance", "transparency", "community"]},
        "penalty": {"type": "integer", "minimum": 0, "maximum": 40},
        "statusAtScan": {"type": "string", "enum": ["shadow", "active"]},
        "filePath": {"type": "string", "maxLength": 1024},
        "lineStart": {"type": "integer", "minimum": 1},
        "lineEnd": {"type": ["integer", "null"], "minimum": 1},
        "matchedContentSha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
        "remediationLink": {"type": "string", "format": "uri"},
        "rubricVersion": {"type": "string", "pattern": "^[a-f0-9]{7,40}$"},
        "evidenceExcerpt": {"$ref": "#/$defs/evidenceExcerpt"}
      }
    },
    "evidenceExcerpt": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["file", "truncated", "lines"],
      "description": "Report-DTO-only matched-line window, resolved from the stored snapshot (verbatim bytes — invisible chars preserved for client-side reveal). NOT a scan-trace field and NOT persisted on the findings table; the trace stays hash-only per security.md § Scan-trace transparency. Null when bytes are absent (binary / oversize / expired snapshot).",
      "properties": {
        "file": {"type": "string", "maxLength": 1024, "description": "Path the excerpt was taken from."},
        "lang": {"type": ["string", "null"], "description": "Language hint from the file extension (display only)."},
        "truncated": {"type": "boolean", "description": "True if the line window or any line was capped."},
        "lines": {
          "type": "array",
          "description": "Ordered line window around the match.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["lineNo", "text", "hit"],
            "properties": {
              "lineNo": {"type": "integer", "minimum": 1, "description": "1-indexed source line number."},
              "text": {"type": "string", "description": "Verbatim line text (bytes preserved; client reveals invisibles)."},
              "hit": {"type": "boolean", "description": "True if this line is within [lineStart, lineEnd]."}
            }
          }
        }
      }
    }
  },
  "examples": [
    {
      "id": "018e7c8b-9999-7000-8000-000000000001",
      "githubUrl": "https://github.com/acme/devtools-agent-kit",
      "repoAggregateScore": 86,
      "repoTier": "green",
      "kindTally": {"skill": 3, "hook": 2, "mcp_server": 1},
      "capabilityCount": 6,
      "capabilities": [
        {
          "kind": "skill",
          "name": "code-review",
          "componentPath": "skills/code-review",
          "aggregateScore": 91,
          "tier": "green",
          "scanId": "018e7c8b-aaaa-7000-8000-000000000001",
          "catalogSlug": "acme--devtools-agent-kit--skill-code-review",
          "subScores": {"security": 95, "supply_chain": 90, "maintenance": 88, "transparency": 90, "community": 92},
          "findingsSummary": {"critical": 0, "high": 0, "medium": 1, "low": 1, "info": 0, "total": 2},
          "findings": []
        }
      ],
      "scannedAt": "2026-06-01T10:30:00Z",
      "rubricVersion": "a1b2c3d",
      "engineVersion": "def5678",
      "latencyMs": 4200,
      "source": "submission",
      "status": "completed",
      "refSha": "abc1234def5678901234567890abcdef12345678"
    }
  ]
}
