//! Shared leaf modules: config + local state, the error model, the HTTP client,
//! the install registry, and telemetry.

pub mod config;
pub mod error;
pub mod http;
pub mod registry;
pub mod telemetry;
