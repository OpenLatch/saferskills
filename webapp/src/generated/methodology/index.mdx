{/* AUTO-GENERATED by scripts/generate-methodology.cjs from rubric/. DO NOT EDIT. */}
{/* rubricSha: 55a0678a4d160d9c2fef38dc3c774c1d7ceb475d */}

import RuleCard from '@/components/methodology/RuleCard.astro'
import RuleGroup from '@/components/methodology/RuleGroup.astro'

export const ruleCount = 55

<RuleGroup category="security" title="Security" weight={35} count={34}>
  <RuleCard
    ruleId="SS-HOOKS-RCE-CURL-PIPE-01"
    title="Hook pipes a remote script straight into a shell"
    categoryLabel="Remote code execution"
    description="This hook runs automatically on an agent event, with no chance for you to review it first. The spotted command &lt;code>the flagged value&lt;/code> pipes whatever the remote server returns at that moment straight into a shell — if the URL is ever compromised, attacker code runs on your machine."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-78","label":"OS Command Injection","url":"https://cwe.mitre.org/data/definitions/78.html"}]}
    triggerSummary="regex `(?i)\b(?:curl|wget|fetch|invoke-webrequest|iwr)\b[^|;\n]*\|\s*(?:bash|sh|zsh|fish|powershell|pwsh|cmd|python|node|perl|ruby)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/*.hook.ps1, **/*.hook.bat, **/SessionStart*, **/SessionEnd*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Cannot detect the pattern when curl output is written to a file first then executed in a separate command. The two-step variant requires a sequence-aware analyzer (deferred to v2).","Cannot detect dynamic-URL construction (e.g. curl \"$BASE/install.sh\" | bash where $BASE is variable).","PowerShell IWR pipelines have many equivalent phrasings; the regex covers the canonical ones."]}
    searchIndex="ss-hooks-rce-curl-pipe-01 hook pipes a remote script straight into a shell this hook runs automatically on an agent event, with no chance for you to review it first. the spotted command the flagged value pipes whatever the remote server returns at that moment straight into a shell — if the url is ever compromised, attacker code runs on your machine. remote code execution llm06 excessive agency aml.t0050 command and scripting interpreter cwe-78 os command injection regex `(?i)\b(?:curl|wget|fetch|invoke-webrequest|iwr)\b[^|;\n]*\|\s*(?:bash|sh|zsh|fish|powershell|pwsh|cmd|python|node|perl|ruby)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/*.hook.ps1, **/*.hook.bat, **/sessionstart*, **/sessionend*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/RCE-CURL-PIPE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-RCE-RMRF-01"
    title="Hook recursively force-deletes a risky path"
    categoryLabel="Command execution"
    description="This hook runs automatically on an agent event, with no chance for you to stop it. The spotted command &lt;code>the flagged value&lt;/code> recursively force-deletes a root, home, or variable-expanded path — if the path resolves wrong at runtime, it irreversibly destroys data."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-78","label":"OS Command Injection","url":"https://cwe.mitre.org/data/definitions/78.html"}]}
    triggerSummary="regex `(?i)\brm\s+(?:-[rRfF]+\s+|--recursive\s+|--force\s+)+(?:/(?:\s|$|\*|[a-zA-Z][a-zA-Z0-9/_-]*)|\$\w+|~/?\s*$|&quot;\$\{?\w+\}?&quot;)` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/*.hook.ps1, **/SessionStart*, **/SessionEnd*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Cannot detect `rm -rf` with paths constructed via complex variable expansion (the rule catches `$VAR` directly but not `\"${BASE}/${SUBDIR}\"` chains).","Cannot detect PowerShell `Remove-Item -Recurse -Force` equivalents — that pattern is a v2 extension.","Cannot distinguish a `rm -rf` that legitimately cleans a known temp directory from one that destroys user data; the rule treats hook-scope `rm -rf` as universally suspicious."]}
    searchIndex="ss-hooks-rce-rmrf-01 hook recursively force-deletes a risky path this hook runs automatically on an agent event, with no chance for you to stop it. the spotted command the flagged value recursively force-deletes a root, home, or variable-expanded path — if the path resolves wrong at runtime, it irreversibly destroys data. command execution llm06 excessive agency aml.t0050 command and scripting interpreter cwe-78 os command injection regex `(?i)\brm\s+(?:-[rrff]+\s+|--recursive\s+|--force\s+)+(?:/(?:\s|$|\*|[a-za-z][a-za-z0-9/_-]*)|\$\w+|~/?\s*$|&quot;\$\{?\w+\}?&quot;)` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/*.hook.ps1, **/sessionstart*, **/sessionend*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/RCE-RMRF-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-POISON-BIDI-01"
    title="BiDi-override characters reorder text in an MCP tool description"
    categoryLabel="Prompt injection"
    description="An MCP tool's &lt;code>description&lt;/code> is read by the agent as trusted instructions. This one embeds bidirectional-override characters (&lt;code>the flagged value&lt;/code>) so the manifest renders one thing to a human reviewer but tokenizes a different, reordered instruction to the agent."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{202A}-\u{202E}\u{2066}-\u{2069}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/server.py, **/server.ts, **/server.js, **/*.toolmanifest.json"
    limitations={["Cannot detect BiDi attacks via inherent-RTL scripts (Arabic / Hebrew text used in MCP tools for non-English users).","Cannot distinguish legitimate i18n use of BiDi overrides from injection."]}
    searchIndex="ss-mcp-poison-bidi-01 bidi-override characters reorder text in an mcp tool description an mcp tool's description is read by the agent as trusted instructions. this one embeds bidirectional-override characters ( the flagged value ) so the manifest renders one thing to a human reviewer but tokenizes a different, reordered instruction to the agent. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{202a}-\u{202e}\u{2066}-\u{2069}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/server.py, **/server.ts, **/server.js, **/*.toolmanifest.json"
    sourcePath="rubric/MCP/POISON-BIDI-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-POISON-DESCRIPTION-CREEP-01"
    title="Oversized MCP tool description (2000+ characters)"
    categoryLabel="Tool poisoning"
    description="An MCP tool's &lt;code>description&lt;/code> is injected into the agent's context as trusted instructions. This one runs past 2000 characters — far beyond the 100–500 a real tool needs — room to bury reasoning chains or system-prompt overrides a human reviewer skims past."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?is)&quot;description&quot;\s*:\s*&quot;[^&quot;]{2000,}&quot;` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    limitations={["Length-based heuristic — does not analyze the description content. A legitimate complex tool may have a long description; vendor-appeal data will tune the threshold.","Cannot detect description-creep injected via dynamic construction (server-side string concatenation).","Cannot detect multi-line JSON-formatted descriptions where the length is hidden by line wrapping in the source view."]}
    searchIndex="ss-mcp-poison-description-creep-01 oversized mcp tool description (2000+ characters) an mcp tool's description is injected into the agent's context as trusted instructions. this one runs past 2000 characters — far beyond the 100–500 a real tool needs — room to bury reasoning chains or system-prompt overrides a human reviewer skims past. tool poisoning llm01 prompt injection aml.t0051 llm prompt injection regex `(?is)&quot;description&quot;\s*:\s*&quot;[^&quot;]{2000,}&quot;` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    sourcePath="rubric/MCP/POISON-DESCRIPTION-CREEP-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-POISON-SHADOW-TOOL-01"
    title="Hidden-looking MCP tool name signals a shadow tool"
    categoryLabel="Tool poisoning"
    description="This server publishes a tool whose name (&lt;code>the flagged value&lt;/code>) uses an &quot;internal/private&quot; naming convention. The agent still sees and may invoke it, but a human reviewing the manifest skips over names that visually signal &quot;not for me&quot; — letting a hidden tool act unreviewed."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?is)&quot;name&quot;\s*:\s*&quot;[^&quot;]*(?:_internal|__|\.hidden|_meta|_sys)[^&quot;]*&quot;` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    limitations={["Heuristic-based: detects tool naming conventions suggestive of hidden / shadow tools. Cannot detect a shadow tool with a normal-looking name that becomes hidden via runtime registration order.","Cannot detect shadow tools registered exclusively at runtime (no entry in the static manifest)."]}
    searchIndex="ss-mcp-poison-shadow-tool-01 hidden-looking mcp tool name signals a shadow tool this server publishes a tool whose name ( the flagged value ) uses an &quot;internal/private&quot; naming convention. the agent still sees and may invoke it, but a human reviewing the manifest skips over names that visually signal &quot;not for me&quot; — letting a hidden tool act unreviewed. tool poisoning llm01 prompt injection aml.t0051 llm prompt injection regex `(?is)&quot;name&quot;\s*:\s*&quot;[^&quot;]*(?:_internal|__|\.hidden|_meta|_sys)[^&quot;]*&quot;` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    sourcePath="rubric/MCP/POISON-SHADOW-TOOL-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-POISON-UNICODE-TAG-01"
    title="Invisible Unicode tag characters hidden in an MCP tool description"
    categoryLabel="Prompt injection"
    description="An MCP tool's &lt;code>description&lt;/code> is fed into the agent's context as trusted instructions. This one hides invisible plane-14 tag characters (&lt;code>the flagged value&lt;/code>) the agent tokenizes as commands, while a human reading the manifest sees the description unchanged."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{E0000}-\u{E007F}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/server.py, **/server.ts, **/server.js, **/*.toolmanifest.json"
    limitations={["Cannot detect tag-channel characters re-encoded as numeric entities; rule operates on decoded text only.","Cannot detect tag-channel chars embedded only in runtime-constructed tool descriptions (e.g. a tool whose description is f-stringed from user input).","Server-source detection is regex-based; obfuscation via string concatenation evades the rule."]}
    searchIndex="ss-mcp-poison-unicode-tag-01 invisible unicode tag characters hidden in an mcp tool description an mcp tool's description is fed into the agent's context as trusted instructions. this one hides invisible plane-14 tag characters ( the flagged value ) the agent tokenizes as commands, while a human reading the manifest sees the description unchanged. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{e0000}-\u{e007f}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/server.py, **/server.ts, **/server.js, **/*.toolmanifest.json"
    sourcePath="rubric/MCP/POISON-UNICODE-TAG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-PLUGIN-SECRET-EXFIL-AWS-FILES-01"
    title="Reads your AWS credentials file"
    categoryLabel="Credential exfiltration"
    description="This plugin references the AWS credentials file or the access-key fields stored inside it (the flagged value). Those are long-lived keys with broad cloud access, so any code that reads them can hand your whole AWS account to whatever it contacts next."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM02","label":"Sensitive Information Disclosure","url":"https://genai.owasp.org/llmrisk/llm022025-sensitive-information-disclosure/"},{"family":"mitre-atlas","id":"AML.T0025","label":"Exfiltration via Cyber Means","url":"https://atlas.mitre.org/techniques/AML.T0025"},{"family":"cwe","id":"CWE-200","label":"Exposure of Sensitive Information","url":"https://cwe.mitre.org/data/definitions/200.html"}]}
    triggerSummary="regex `(?i)(~/.aws/credentials|~/.aws/config|/\.aws/credentials|aws_access_key_id|aws_secret_access_key|aws_session_token)` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java"
    limitations={["Legitimate plugins that interact with AWS (deployment tools, S3 clients, etc.) need to reference the credentials file path. The rule's coarse detection means FP risk on every AWS-using plugin.","Cannot distinguish a plugin that reads .aws/credentials in a documented, user-consented flow from one that does so for exfiltration.","v2 will refine via composite (AWS-file read PLUS unexpected-endpoint HTTP call)."]}
    searchIndex="ss-plugin-secret-exfil-aws-files-01 reads your aws credentials file this plugin references the aws credentials file or the access-key fields stored inside it (the flagged value). those are long-lived keys with broad cloud access, so any code that reads them can hand your whole aws account to whatever it contacts next. credential exfiltration llm02 sensitive information disclosure aml.t0025 exfiltration via cyber means cwe-200 exposure of sensitive information regex `(?i)(~/.aws/credentials|~/.aws/config|/\.aws/credentials|aws_access_key_id|aws_secret_access_key|aws_session_token)` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java"
    sourcePath="rubric/PLUGIN/SECRET-EXFIL-AWS-FILES-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-PLUGIN-SECRET-EXFIL-ENV-NET-01"
    title="Reads environment variables and makes outbound HTTP calls in the same plugin"
    categoryLabel="Credential exfiltration"
    description="This plugin reads from the environment (where API keys, tokens, and AWS credentials live) and also makes outbound network calls. When both sit in the same code, a secret read from the env can be packed into a request and sent off the machine the moment the plugin runs."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM02","label":"Sensitive Information Disclosure","url":"https://genai.owasp.org/llmrisk/llm022025-sensitive-information-disclosure/"},{"family":"mitre-atlas","id":"AML.T0025","label":"Exfiltration via Cyber Means","url":"https://atlas.mitre.org/techniques/AML.T0025"},{"family":"cwe","id":"CWE-200","label":"Exposure of Sensitive Information","url":"https://cwe.mitre.org/data/definitions/200.html"}]}
    triggerSummary="AND of 2 sub-triggers"
    limitations={["Composite trigger detects only the *coexistence* of env-read and HTTP-call primitives in the same plugin. Cannot prove that env values flow into HTTP bodies (taint analysis is deferred to v2).","FP risk on legitimate plugins that legitimately read env (e.g. an API key) AND call out (e.g. to the API endpoint that requires the key). The pattern is normal for any service-integration plugin; treat the rule as a signal-to-review, not a verdict.","Cannot detect dynamic env access via Reflect / getattr / runtime-string."]}
    searchIndex="ss-plugin-secret-exfil-env-net-01 reads environment variables and makes outbound http calls in the same plugin this plugin reads from the environment (where api keys, tokens, and aws credentials live) and also makes outbound network calls. when both sit in the same code, a secret read from the env can be packed into a request and sent off the machine the moment the plugin runs. credential exfiltration llm02 sensitive information disclosure aml.t0025 exfiltration via cyber means cwe-200 exposure of sensitive information and of 2 sub-triggers"
    sourcePath="rubric/PLUGIN/SECRET-EXFIL-ENV-NET-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-PLUGIN-SECRET-EXFIL-GH-TOKEN-01"
    title="Contains a committed GitHub token"
    categoryLabel="Credential exfiltration"
    description="A GitHub token (the flagged value) is committed directly into this plugin's source. Anyone who reads the repo — including everyone who installs the plugin — gets the token, so it must be treated as already compromised."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM02","label":"Sensitive Information Disclosure","url":"https://genai.owasp.org/llmrisk/llm022025-sensitive-information-disclosure/"},{"family":"mitre-atlas","id":"AML.T0025","label":"Exfiltration via Cyber Means","url":"https://atlas.mitre.org/techniques/AML.T0025"},{"family":"cwe","id":"CWE-798","label":"Use of Hard-coded Credentials","url":"https://cwe.mitre.org/data/definitions/798.html"}]}
    triggerSummary="regex `\b(?:ghp_[A-Za-z0-9]{36}|github_pat_[A-Za-z0-9_]{82}|gho_[A-Za-z0-9]{36}|ghu_[A-Za-z0-9]{36}|ghs_[A-Za-z0-9]{36}|ghr_[A-Za-z0-9]{36})\b` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java, **/*.md, **/*.json, **/*.yaml, **/*.yml"
    limitations={["Detects only the canonical GitHub token prefixes (ghp_ / github_pat_ / gho_ / ghu_ / ghs_ / ghr_). Cannot detect legacy 40-char hex tokens without high FP risk.","Detects committed tokens; cannot detect tokens read at runtime from env or from external secrets store.","Cannot distinguish revoked / expired tokens from active ones."]}
    searchIndex="ss-plugin-secret-exfil-gh-token-01 contains a committed github token a github token (the flagged value) is committed directly into this plugin's source. anyone who reads the repo — including everyone who installs the plugin — gets the token, so it must be treated as already compromised. credential exfiltration llm02 sensitive information disclosure aml.t0025 exfiltration via cyber means cwe-798 use of hard-coded credentials regex `\b(?:ghp_[a-za-z0-9]{36}|github_pat_[a-za-z0-9_]{82}|gho_[a-za-z0-9]{36}|ghu_[a-za-z0-9]{36}|ghs_[a-za-z0-9]{36}|ghr_[a-za-z0-9]{36})\b` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java, **/*.md, **/*.json, **/*.yaml, **/*.yml"
    sourcePath="rubric/PLUGIN/SECRET-EXFIL-GH-TOKEN-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-RULES-OBFUSCATION-UNICODE-TAG-01"
    title="Rules file hides instructions in invisible Unicode tag characters"
    categoryLabel="Obfuscation"
    description="A rules file is injected verbatim into every agent prompt. Unicode tag-channel codepoints like the flagged value render as nothing to a human reviewer but are tokenized as text by the model — so hidden instructions steer every session while staying invisible in any editor or diff."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["rules"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{E0000}-\u{E007F}]` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, CONTINUE.md, **/rules.md, **/RULES.md"
    limitations={["Cannot detect tag-channel characters re-encoded as numeric entities.","Cannot detect tag-channel chars in dynamically-loaded rules (e.g. rules constructed at IDE-runtime)."]}
    searchIndex="ss-rules-obfuscation-unicode-tag-01 rules file hides instructions in invisible unicode tag characters a rules file is injected verbatim into every agent prompt. unicode tag-channel codepoints like the flagged value render as nothing to a human reviewer but are tokenized as text by the model — so hidden instructions steer every session while staying invisible in any editor or diff. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{e0000}-\u{e007f}]` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, continue.md, **/rules.md, **/rules.md"
    sourcePath="rubric/RULES/OBFUSCATION-UNICODE-TAG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-BIDI-01"
    title="Bidirectional-override characters that make the text read differently than the agent sees it"
    categoryLabel="Obfuscation"
    description="A Right-to-Left Override or isolate character (U+202A–U+202E, U+2066–U+2069) reorders how the line displays without changing what the tokenizer reads — the Trojan Source trick. The file can look harmless to you while injecting hostile instructions at the token level, and the deception survives copy-paste into a sandbox."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{202A}-\u{202E}\u{2066}-\u{2069}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md, **/*.py, **/*.ts, **/*.js, **/*.sh"
    limitations={["Cannot detect BiDi attacks that use RTL-marker-free scripts (Arabic / Hebrew script blocks have inherent RTL); rule scopes to explicit BiDi-override codepoints only.","Cannot distinguish legitimate i18n use (mixed-script documentation) from injection — fires on any explicit override character. Reviewers should examine context."]}
    searchIndex="ss-skill-inject-bidi-01 bidirectional-override characters that make the text read differently than the agent sees it a right-to-left override or isolate character (u+202a–u+202e, u+2066–u+2069) reorders how the line displays without changing what the tokenizer reads — the trojan source trick. the file can look harmless to you while injecting hostile instructions at the token level, and the deception survives copy-paste into a sandbox. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{202a}-\u{202e}\u{2066}-\u{2069}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md, **/*.py, **/*.ts, **/*.js, **/*.sh"
    sourcePath="rubric/SKILL/INJECT-BIDI-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-UNICODE-TAG-01"
    title="Invisible Unicode &quot;tag&quot; characters hidden in the instructions"
    categoryLabel="Obfuscation"
    description="Plane-14 tag characters (U+E0000–U+E007F) render as absolutely nothing to a person reading the file in any editor, yet every LLM tokenizer turns them into real tokens. An attacker can hide a whole second instruction in them — telling the agent to drop its safety rules or exfiltrate data — that no reviewer ever sees in the source."
    severity="critical"
    subScore="security"
    status="active"
    weight={35}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{E0000}-\u{E007F}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md"
    limitations={["Cannot detect tag-channel characters re-encoded as numeric entities (`&#xE0001;`); rule operates on decoded text only.","Cannot detect tag-channel chars hidden inside compressed payloads (gzip / brotli); only post-decompression bytes are scanned.","Does not distinguish documented use of plane-14 characters (rare) from injection — fires on any presence."]}
    searchIndex="ss-skill-inject-unicode-tag-01 invisible unicode &quot;tag&quot; characters hidden in the instructions plane-14 tag characters (u+e0000–u+e007f) render as absolutely nothing to a person reading the file in any editor, yet every llm tokenizer turns them into real tokens. an attacker can hide a whole second instruction in them — telling the agent to drop its safety rules or exfiltrate data — that no reviewer ever sees in the source. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{e0000}-\u{e007f}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-UNICODE-TAG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-OBFUSCATION-B64-SHELL-01"
    title="Hook decodes a Base64 blob and runs it as shell"
    categoryLabel="Obfuscation"
    description="This hook runs automatically on an agent event. The spotted command &lt;code>the flagged value&lt;/code> Base64-decodes a blob and pipes it straight into a shell — encoding that hides the real commands from review, with no legitimate reason for a hook to obscure its own plain text."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-78","label":"OS Command Injection","url":"https://cwe.mitre.org/data/definitions/78.html"}]}
    triggerSummary="regex `(?i)\b(?:echo|printf)\s+[&quot;']?[A-Za-z0-9+/=]{32,}[&quot;']?\s*\|\s*(?:base64\s+(?:-d|--decode)|openssl\s+base64\s+-d)\s*\|\s*(?:bash|sh|zsh)\b|\bbase64\s+(?:-d|--decode)\s+&lt;&lt;&lt;\s*[&quot;'][A-Za-z0-9+/=]{32,}[&quot;']\s*\|\s*(?:bash|sh)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/SessionStart*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Cannot detect base64-shell pipelines that split the encoded payload across variables before piping.","Cannot detect hex-encoded or otherwise-encoded shell payloads (those would be a separate rule).","Cannot detect base64-decode chains that route through a temp file before execution."]}
    searchIndex="ss-hooks-obfuscation-b64-shell-01 hook decodes a base64 blob and runs it as shell this hook runs automatically on an agent event. the spotted command the flagged value base64-decodes a blob and pipes it straight into a shell — encoding that hides the real commands from review, with no legitimate reason for a hook to obscure its own plain text. obfuscation llm06 excessive agency aml.t0050 command and scripting interpreter cwe-78 os command injection regex `(?i)\b(?:echo|printf)\s+[&quot;']?[a-za-z0-9+/=]{32,}[&quot;']?\s*\|\s*(?:base64\s+(?:-d|--decode)|openssl\s+base64\s+-d)\s*\|\s*(?:bash|sh|zsh)\b|\bbase64\s+(?:-d|--decode)\s+&lt;&lt;&lt;\s*[&quot;'][a-za-z0-9+/=]{32,}[&quot;']\s*\|\s*(?:bash|sh)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/sessionstart*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/OBFUSCATION-B64-SHELL-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-OBFUSCATION-EVAL-01"
    title="Hook builds and runs commands at runtime with eval"
    categoryLabel="Obfuscation"
    description="This hook runs automatically on an agent event. The spotted command &lt;code>the flagged value&lt;/code> uses &lt;code>eval&lt;/code> on command-substituted or variable content — the actual code is assembled at runtime from values not visible in the source, defeating any static review."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-95","label":"Eval Injection","url":"https://cwe.mitre.org/data/definitions/95.html"}]}
    triggerSummary="regex `(?i)\beval\s+[&quot;']?\$\(.*\)|\beval\s+[&quot;']?[^&quot;'\n]*\$\{?\w+\}?[^&quot;'\n]*[&quot;']?|\bsource\s+&lt;\(.+\)` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/SessionStart*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Legitimate uses of eval exist (string-substitution before execution); the FP risk is real but the hook-scope filter bounds it.","Cannot detect non-shell eval equivalents (Python exec(), JavaScript eval(), etc.) in hook scripts written in those languages — those would be separate rules.","Cannot detect indirect-eval (functions whose body executes a variable)."]}
    searchIndex="ss-hooks-obfuscation-eval-01 hook builds and runs commands at runtime with eval this hook runs automatically on an agent event. the spotted command the flagged value uses eval on command-substituted or variable content — the actual code is assembled at runtime from values not visible in the source, defeating any static review. obfuscation llm06 excessive agency aml.t0050 command and scripting interpreter cwe-95 eval injection regex `(?i)\beval\s+[&quot;']?\$\(.*\)|\beval\s+[&quot;']?[^&quot;'\n]*\$\{?\w+\}?[^&quot;'\n]*[&quot;']?|\bsource\s+&lt;\(.+\)` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/sessionstart*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/OBFUSCATION-EVAL-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-RCE-NET-EGRESS-01"
    title="Hook opens a reverse shell or raw outbound socket"
    categoryLabel="Remote code execution"
    description="This hook runs automatically on an agent event. The spotted command &lt;code>the flagged value&lt;/code> wires a shell to an outbound TCP socket (netcat or &lt;code>/dev/tcp&lt;/code>) — a reverse shell that hands an attacker persistent, interactive access to your machine the moment it runs."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-78","label":"OS Command Injection","url":"https://cwe.mitre.org/data/definitions/78.html"}]}
    triggerSummary="regex `(?i)\b(?:nc|netcat|ncat|socat)\s+(?:-[a-z]+\s+)*\S+\s+\d+|/dev/tcp/\S+/\d+|bash\s+-i\s+>&amp;\s*/dev/tcp/` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/SessionStart*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Cannot detect reverse-shell patterns implemented via Python sockets, Node net.connect, or other native runtime APIs (the regex covers shell-level patterns only).","Cannot distinguish a reverse-shell setup from a legitimate use of nc for protocol testing (rare in hook scope, but possible).","Operates on static text only — runtime-constructed connection strings are not detected."]}
    searchIndex="ss-hooks-rce-net-egress-01 hook opens a reverse shell or raw outbound socket this hook runs automatically on an agent event. the spotted command the flagged value wires a shell to an outbound tcp socket (netcat or /dev/tcp ) — a reverse shell that hands an attacker persistent, interactive access to your machine the moment it runs. remote code execution llm06 excessive agency aml.t0050 command and scripting interpreter cwe-78 os command injection regex `(?i)\b(?:nc|netcat|ncat|socat)\s+(?:-[a-z]+\s+)*\s+\s+\d+|/dev/tcp/\s+/\d+|bash\s+-i\s+>&amp;\s*/dev/tcp/` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/sessionstart*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/RCE-NET-EGRESS-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-RCE-SUDO-UNATTENDED-01"
    title="Hook escalates to root without prompting you"
    categoryLabel="Command execution"
    description="This hook runs automatically on an agent event. The spotted command &lt;code>the flagged value&lt;/code> invokes &lt;code>sudo&lt;/code> in a way that skips the password prompt (a piped password, &lt;code>-n&lt;/code>/&lt;code>-S&lt;/code>, or NOPASSWD) — so it can run as root without you ever confirming."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-250","label":"Execution with Unnecessary Privileges","url":"https://cwe.mitre.org/data/definitions/250.html"}]}
    triggerSummary="regex `(?i)\bsudo\s+(?:-[ASEnk]+\s+)?\b|\becho\s+[&quot;']?\$[A-Z_]+[&quot;']?\s*\|\s*sudo\s+-S\b|\bNOPASSWD\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/SessionStart*, **/PreToolUse*"
    limitations={["Legitimate use case: installation hooks for tools that genuinely require root (rare in agent-tool ecosystems). Shadow window measures real-world FP.","Cannot distinguish a sudo invocation that would prompt the user (acceptable) from a sudo invocation backed by NOPASSWD or a piped-password (unacceptable).","Does not cover Windows runas / elevation equivalents — v2 extension."]}
    searchIndex="ss-hooks-rce-sudo-unattended-01 hook escalates to root without prompting you this hook runs automatically on an agent event. the spotted command the flagged value invokes sudo in a way that skips the password prompt (a piped password, -n / -s , or nopasswd) — so it can run as root without you ever confirming. command execution llm06 excessive agency aml.t0050 command and scripting interpreter cwe-250 execution with unnecessary privileges regex `(?i)\bsudo\s+(?:-[asenk]+\s+)?\b|\becho\s+[&quot;']?\$[a-z_]+[&quot;']?\s*\|\s*sudo\s+-s\b|\bnopasswd\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/sessionstart*, **/pretooluse*"
    sourcePath="rubric/HOOKS/RCE-SUDO-UNATTENDED-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-CAP-UNDECLARED-01"
    title="MCP server spawns subprocesses without declaring the capability"
    categoryLabel="Undeclared capability"
    description="The server's source spawns processes (e.g. &lt;code>subprocess&lt;/code>, &lt;code>child_process&lt;/code>, &lt;code>exec()&lt;/code>) but its manifest never declares that capability, so the consuming client can't warn the user that this server may run shell commands on their machine."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0053","label":"AI Agent Tool Invocation","url":"https://atlas.mitre.org/techniques/AML.T0053"}]}
    triggerSummary="AND of 2 sub-triggers"
    limitations={["Heuristic detection of process-spawning primitives — cannot detect dynamically-loaded process libraries (e.g. importlib-resolved subprocess wrappers).","Manifest capability declarations are advisory; the MCP protocol does not enforce them. The rule flags discrepancy as a transparency signal, not a hard guarantee.","FP risk on MCP servers that subprocess only for legitimate internal lifecycle (worker management, not user-facing tool execution). Shadow window measures."]}
    searchIndex="ss-mcp-cap-undeclared-01 mcp server spawns subprocesses without declaring the capability the server's source spawns processes (e.g. subprocess , child_process , exec() ) but its manifest never declares that capability, so the consuming client can't warn the user that this server may run shell commands on their machine. undeclared capability llm06 excessive agency aml.t0053 ai agent tool invocation and of 2 sub-triggers"
    sourcePath="rubric/MCP/CAP-UNDECLARED-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-POISON-ZWSP-01"
    title="Zero-width characters hidden in an MCP tool manifest"
    categoryLabel="Obfuscation"
    description="An MCP tool's description is read by the agent as trusted instructions. This manifest packs three or more invisible zero-width characters (&lt;code>the flagged value&lt;/code>) that the agent tokenizes but a human reviewer cannot see — a way to smuggle hidden instructions past review."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{200B}-\u{200D}\u{2060}\u{FEFF}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    limitations={["Requires ≥3 zero-width characters to fire (FP reduction).","Cannot detect zero-width characters re-encoded as numeric entities.","Cannot detect zero-width characters in dynamically-constructed tool descriptions."]}
    searchIndex="ss-mcp-poison-zwsp-01 zero-width characters hidden in an mcp tool manifest an mcp tool's description is read by the agent as trusted instructions. this manifest packs three or more invisible zero-width characters ( the flagged value ) that the agent tokenizes but a human reviewer cannot see — a way to smuggle hidden instructions past review. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{200b}-\u{200d}\u{2060}\u{feff}]` against tools/manifest.json, **/tools/manifest.json, manifest.json, **/*.toolmanifest.json"
    sourcePath="rubric/MCP/POISON-ZWSP-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-PLUGIN-SECRET-EXFIL-SSH-01"
    title="Reads your SSH private key"
    categoryLabel="Credential exfiltration"
    description="This plugin references an SSH private-key path or a private-key file header (the flagged value). An SSH private key authenticates you to servers and Git remotes, so code that reads it can impersonate you wherever that key is trusted."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM02","label":"Sensitive Information Disclosure","url":"https://genai.owasp.org/llmrisk/llm022025-sensitive-information-disclosure/"},{"family":"mitre-atlas","id":"AML.T0025","label":"Exfiltration via Cyber Means","url":"https://atlas.mitre.org/techniques/AML.T0025"},{"family":"cwe","id":"CWE-200","label":"Exposure of Sensitive Information","url":"https://cwe.mitre.org/data/definitions/200.html"}]}
    triggerSummary="regex `(?i)(~/.ssh/id_rsa|~/.ssh/id_ed25519|~/.ssh/id_ecdsa|/\.ssh/id_[a-z]+|BEGIN\s+(?:RSA|OPENSSH|DSA|EC)\s+PRIVATE\s+KEY)` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java"
    limitations={["Legitimate SSH-using plugins (git wrappers, SSH-key-rotation tools) reference the canonical paths.","Cannot detect runtime-resolved key paths (e.g. paths read from config).","Cannot detect direct memory read of the SSH agent."]}
    searchIndex="ss-plugin-secret-exfil-ssh-01 reads your ssh private key this plugin references an ssh private-key path or a private-key file header (the flagged value). an ssh private key authenticates you to servers and git remotes, so code that reads it can impersonate you wherever that key is trusted. credential exfiltration llm02 sensitive information disclosure aml.t0025 exfiltration via cyber means cwe-200 exposure of sensitive information regex `(?i)(~/.ssh/id_rsa|~/.ssh/id_ed25519|~/.ssh/id_ecdsa|/\.ssh/id_[a-z]+|begin\s+(?:rsa|openssh|dsa|ec)\s+private\s+key)` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java"
    sourcePath="rubric/PLUGIN/SECRET-EXFIL-SSH-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-PLUGIN-SECRET-EXFIL-WEBHOOK-01"
    title="Sends data to a hardcoded chat or capture webhook"
    categoryLabel="Credential exfiltration"
    description="This plugin embeds a chat-platform or request-capture webhook URL (the flagged value). Webhooks are the classic exfiltration drop: a plugin collects env, files, or system info and posts it to a hardcoded endpoint the attacker watches."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM02","label":"Sensitive Information Disclosure","url":"https://genai.owasp.org/llmrisk/llm022025-sensitive-information-disclosure/"},{"family":"mitre-atlas","id":"AML.T0025","label":"Exfiltration via Cyber Means","url":"https://atlas.mitre.org/techniques/AML.T0025"},{"family":"cwe","id":"CWE-200","label":"Exposure of Sensitive Information","url":"https://cwe.mitre.org/data/definitions/200.html"}]}
    triggerSummary="regex `(?i)https://(?:hooks\.slack\.com/services/|discord(?:app)?\.com/api/webhooks/|outlook\.office\.com/webhook/|api\.telegram\.org/bot|webhook\.site/|requestcatcher\.com/|pipedream\.com/|n8n\.cloud/|zapier\.com/hooks/)\S+` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java, **/*.md, **/*.json, **/*.yaml, **/*.yml"
    limitations={["Many legitimate plugins use webhook URLs (Slack notifiers, Discord bots, monitoring integrations). Shadow window measures real-world FP cost.","Cannot detect webhook URLs constructed at runtime from variables.","Cannot distinguish a user-configured legitimate webhook from a hardcoded exfiltration endpoint without consulting the rest of the plugin source for the configuration mechanism."]}
    searchIndex="ss-plugin-secret-exfil-webhook-01 sends data to a hardcoded chat or capture webhook this plugin embeds a chat-platform or request-capture webhook url (the flagged value). webhooks are the classic exfiltration drop: a plugin collects env, files, or system info and posts it to a hardcoded endpoint the attacker watches. credential exfiltration llm02 sensitive information disclosure aml.t0025 exfiltration via cyber means cwe-200 exposure of sensitive information regex `(?i)https://(?:hooks\.slack\.com/services/|discord(?:app)?\.com/api/webhooks/|outlook\.office\.com/webhook/|api\.telegram\.org/bot|webhook\.site/|requestcatcher\.com/|pipedream\.com/|n8n\.cloud/|zapier\.com/hooks/)\s+` against **/*.py, **/*.ts, **/*.js, **/*.mjs, **/*.cjs, **/*.go, **/*.rb, **/*.java, **/*.md, **/*.json, **/*.yaml, **/*.yml"
    sourcePath="rubric/PLUGIN/SECRET-EXFIL-WEBHOOK-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-B64-PAYLOAD-01"
    title="Long base64-encoded blob hidden in the skill documentation"
    categoryLabel="Prompt injection"
    description="A base64 string of 128+ characters appears in a documentation file. Encoded prompt injection hides the hostile instruction in base64 — invisible to keyword filters — and relies on the agent's ability to decode it at runtime. There is no normal authoring reason to embed a multi-hundred-byte base64 blob in skill docs."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?&lt;![A-Za-z0-9+/=])(?:[A-Za-z0-9+/]{4}){32,}(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?(?![A-Za-z0-9+/=])` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md, **/CLAUDE.md"
    limitations={["Cannot distinguish injection payloads from legitimate base64 (e.g. embedded test fixtures, SHA hashes presented in base64, signed manifests). Operates on the *presence* of a sufficiently long base64-shaped string in a documentation file as a signal.","Does not decode the payload at scan time — the engine flags the pattern; review by maintainer or vendor surfaces the decoded content.","Requires ≥128 base64 chars (32 quartets) to fire. Shorter strings (typical signatures / hashes) do not trigger."]}
    searchIndex="ss-skill-inject-b64-payload-01 long base64-encoded blob hidden in the skill documentation a base64 string of 128+ characters appears in a documentation file. encoded prompt injection hides the hostile instruction in base64 — invisible to keyword filters — and relies on the agent's ability to decode it at runtime. there is no normal authoring reason to embed a multi-hundred-byte base64 blob in skill docs. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?&lt;![a-za-z0-9+/=])(?:[a-za-z0-9+/]{4}){32,}(?:[a-za-z0-9+/]{2}==|[a-za-z0-9+/]{3}=)?(?![a-za-z0-9+/=])` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-B64-PAYLOAD-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-FENCED-RUN-01"
    title="Fenced code block that tells the agent to run a command"
    categoryLabel="Prompt injection"
    description="A fenced &lt;code>bash&lt;/code>/&lt;code>python&lt;/code> block in SKILL.md carries a natural-language imperative — &quot;now run this&quot;, &quot;execute the following command&quot; — directing the agent to execute the fenced content. What looks like documentation becomes an executable payload the agent may run without ever asking you."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?ms)^```(?:bash|sh|zsh|powershell|pwsh|cmd|python|node|js|ts)\s*$\s+(?:.*?\b(?:please|now|then|next|first)\s+(?:run|execute|invoke|call|trigger)\b.*?|.*?(?:run|execute|invoke|call|trigger)\s+(?:this|the\s+(?:above|below|following))\s+(?:command|code|script)\b.*?)```` against **/*.md, **/SKILL.md"
    limitations={["Cannot detect imperative instructions that don't use the curated verb set (run/execute/invoke/call/trigger).","Cannot detect fenced-imperative when the natural-language wrapper is outside the fence (most skills place instructions BEFORE a fence, not inside it).","May over-fire on tutorials demonstrating the safe execution of example code; shadow window measures real-world impact."]}
    searchIndex="ss-skill-inject-fenced-run-01 fenced code block that tells the agent to run a command a fenced bash / python block in skill.md carries a natural-language imperative — &quot;now run this&quot;, &quot;execute the following command&quot; — directing the agent to execute the fenced content. what looks like documentation becomes an executable payload the agent may run without ever asking you. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?ms)^```(?:bash|sh|zsh|powershell|pwsh|cmd|python|node|js|ts)\s*$\s+(?:.*?\b(?:please|now|then|next|first)\s+(?:run|execute|invoke|call|trigger)\b.*?|.*?(?:run|execute|invoke|call|trigger)\s+(?:this|the\s+(?:above|below|following))\s+(?:command|code|script)\b.*?)```` against **/*.md, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-FENCED-RUN-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-HEX-PAYLOAD-01"
    title="Long hex-encoded blob hidden in the skill documentation"
    categoryLabel="Prompt injection"
    description="A hex string of 256+ characters appears in a documentation file — well above a single SHA-256 hash (64 chars). Like its base64 sibling, hex-encoding hides a hostile instruction from keyword filters while staying trivially decodable by the agent at runtime."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?i)(?&lt;![0-9a-f])([0-9a-f]{256,})(?![0-9a-f])` against **/*.md, **/*.yaml, **/*.yml, **/SKILL.md, **/CLAUDE.md"
    limitations={["Cannot distinguish hex-encoded injection payloads from legitimate hex constants (SHA-256 of a known artifact, signed manifest entries). Operates on the *presence* of a sufficiently long hex-shaped string in documentation as a signal.","Requires ≥256 hex chars (128 bytes decoded) to fire — well above typical SHA-256 (64 hex chars) so single-hash inclusions do not trigger.","Does not decode the payload at scan time."]}
    searchIndex="ss-skill-inject-hex-payload-01 long hex-encoded blob hidden in the skill documentation a hex string of 256+ characters appears in a documentation file — well above a single sha-256 hash (64 chars). like its base64 sibling, hex-encoding hides a hostile instruction from keyword filters while staying trivially decodable by the agent at runtime. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?i)(?&lt;![0-9a-f])([0-9a-f]{256,})(?![0-9a-f])` against **/*.md, **/*.yaml, **/*.yml, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-HEX-PAYLOAD-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-IGNORE-01"
    title="&quot;Ignore previous instructions&quot; command embedded in the skill"
    categoryLabel="Prompt injection"
    description="The text &lt;code>the flagged value&lt;/code> is the classic direct prompt-injection phrasing. Placed in a skill body that the agent reads as trusted instructions, it tries to make the agent abandon its prior rules and follow whatever comes next — a full system-prompt override."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?i)\b(ignore|disregard|forget|override)\s+(all\s+)?(previous|prior|above|earlier)\s+(instructions?|prompts?|rules?|system\s+prompts?)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md"
    limitations={["False-positives on roleplay skills, jailbreak research documentation, prompt-engineering tutorials, and any meta-discussion of LLM safety. Shadow window measures real-world FP rate.","Cannot detect non-English equivalents of the imperative pattern. Coverage at W2 is English-only; multilingual extension is a Phase 2 RFC.","Cannot detect the pattern when split across formatting or hidden in comments."]}
    searchIndex="ss-skill-inject-ignore-01 &quot;ignore previous instructions&quot; command embedded in the skill the text the flagged value is the classic direct prompt-injection phrasing. placed in a skill body that the agent reads as trusted instructions, it tries to make the agent abandon its prior rules and follow whatever comes next — a full system-prompt override. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?i)\b(ignore|disregard|forget|override)\s+(all\s+)?(previous|prior|above|earlier)\s+(instructions?|prompts?|rules?|system\s+prompts?)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-IGNORE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-ROLE-01"
    title="Role-override jailbreak persona in the skill text"
    categoryLabel="Prompt injection"
    description="The phrase &lt;code>the flagged value&lt;/code> matches a known role-override jailbreak — DAN, &quot;developer mode&quot;, &quot;jailbroken&quot;, &quot;unrestricted&quot;, or an evil/malicious persona. Embedded in a skill, it tries to push the agent into an adversarial role that drops its safety constraints, without the person running the skill realizing it."
    severity="high"
    subScore="security"
    status="shadow"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?i)\b(you\s+are|act\s+as|pretend\s+(to\s+be|you\s+are)|roleplay\s+as)\s+(DAN|developer\s+mode|jailbroken|unrestricted|uncensored|an?\s+(evil|malicious|harmful|amoral)\s+\w+)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md"
    limitations={["FP rate on jailbreak-research and red-team skills is expected to be non-trivial — shadow window measures real-world impact.","Cannot detect novel role-override personas coined after the rule lands. The DAN/developer-mode/jailbroken seed list is curated; new personas require a v2 rule revision.","Cannot detect role-override embedded in tool-call arguments dynamically constructed at runtime."]}
    searchIndex="ss-skill-inject-role-01 role-override jailbreak persona in the skill text the phrase the flagged value matches a known role-override jailbreak — dan, &quot;developer mode&quot;, &quot;jailbroken&quot;, &quot;unrestricted&quot;, or an evil/malicious persona. embedded in a skill, it tries to push the agent into an adversarial role that drops its safety constraints, without the person running the skill realizing it. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?i)\b(you\s+are|act\s+as|pretend\s+(to\s+be|you\s+are)|roleplay\s+as)\s+(dan|developer\s+mode|jailbroken|unrestricted|uncensored|an?\s+(evil|malicious|harmful|amoral)\s+\w+)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-ROLE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-ZWSP-01"
    title="Zero-width characters smuggled into the instructions"
    categoryLabel="Obfuscation"
    description="Zero-width spaces and joiners (U+200B–U+200D, U+2060, U+FEFF) are invisible to a human reading the file but are emitted as distinct tokens by the agent's tokenizer. Several of them clustered together — this rule fires on three or more — can carry a hidden instruction past a reviewer who sees only ordinary text."
    severity="high"
    subScore="security"
    status="active"
    weight={25}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{200B}-\u{200D}\u{2060}\u{FEFF}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md"
    limitations={["Requires ≥3 zero-width characters to fire (FP reduction). A single zero-width space — e.g. a copy-paste artifact from a Slack message — does not trigger.","Cannot detect zero-width characters re-encoded as numeric entities (`&#x200B;`); rule operates on decoded text only.","Does not currently distinguish between zero-width characters used legitimately in CJK script and the same characters used adversarially."]}
    searchIndex="ss-skill-inject-zwsp-01 zero-width characters smuggled into the instructions zero-width spaces and joiners (u+200b–u+200d, u+2060, u+feff) are invisible to a human reading the file but are emitted as distinct tokens by the agent's tokenizer. several of them clustered together — this rule fires on three or more — can carry a hidden instruction past a reviewer who sees only ordinary text. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{200b}-\u{200d}\u{2060}\u{feff}]` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-ZWSP-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-RCE-CHMOD-WIDE-01"
    title="Hook makes files world-writable"
    categoryLabel="Security"
    description="This hook runs automatically on an agent event. The spotted command &lt;code>the flagged value&lt;/code> grants world-writable permissions (&lt;code>777&lt;/code> / &lt;code>a+rwx&lt;/code>) — breaking least-privilege and leaving the files open for any other process on the system to tamper with."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM06","label":"Excessive Agency","url":"https://genai.owasp.org/llmrisk/llm062025-excessive-agency/"},{"family":"mitre-atlas","id":"AML.T0050","label":"Command and Scripting Interpreter","url":"https://atlas.mitre.org/techniques/AML.T0050"},{"family":"cwe","id":"CWE-732","label":"Incorrect Permission Assignment","url":"https://cwe.mitre.org/data/definitions/732.html"}]}
    triggerSummary="regex `(?i)\bchmod\s+(?:-R\s+)?(?:777|a\+rwx|o\+rwx|0?777)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/SessionStart*, **/PreToolUse*, **/PostToolUse*"
    limitations={["Legitimate use: hooks that prepare a shared-temp-directory or socket for cross-user access. Shadow window measures FP rate.","Cannot detect chmod 666 / 644 / other wide-but-not-fully-permissive patterns that still violate least-privilege.","Cannot detect equivalent setfacl / takeown / icacls patterns on other platforms."]}
    searchIndex="ss-hooks-rce-chmod-wide-01 hook makes files world-writable this hook runs automatically on an agent event. the spotted command the flagged value grants world-writable permissions ( 777 / a+rwx ) — breaking least-privilege and leaving the files open for any other process on the system to tamper with. security llm06 excessive agency aml.t0050 command and scripting interpreter cwe-732 incorrect permission assignment regex `(?i)\bchmod\s+(?:-r\s+)?(?:777|a\+rwx|o\+rwx|0?777)\b` against .claude/hooks/**, hooks/**, **/*.hook.sh, **/sessionstart*, **/pretooluse*, **/posttooluse*"
    sourcePath="rubric/HOOKS/RCE-CHMOD-WIDE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-RULES-INJECT-IMPERATIVE-01"
    title="Rules file commands the agent to hide or withhold information"
    categoryLabel="Prompt injection"
    description="A rules file is loaded verbatim into the agent as standing instructions for every session. A non-disclosure imperative like the flagged value steers the agent to conceal behaviour, hide tool calls, or withhold information on every prompt — never appearing in any single conversation you'd review."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["rules"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?im)^\s*(?:•|-|\*|\d+\.)\s*(?:always|never|must|do\s+not|don[’']t|under\s+no\s+circumstances)\s+(?:reveal|disclose|share|leak|tell\s+(?:the\s+)?(?:user|anyone)|mention)\b` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, CONTINUE.md, **/rules.md, **/RULES.md"
    limitations={["Cursor / Windsurf rule files frequently contain legitimate confidentiality instructions ('always keep API keys out of logs'). Shadow window measures FP rate.","Cannot distinguish user-facing imperatives (legitimate) from model-facing imperatives (suspicious)."]}
    searchIndex="ss-rules-inject-imperative-01 rules file commands the agent to hide or withhold information a rules file is loaded verbatim into the agent as standing instructions for every session. a non-disclosure imperative like the flagged value steers the agent to conceal behaviour, hide tool calls, or withhold information on every prompt — never appearing in any single conversation you'd review. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?im)^\s*(?:•|-|\*|\d+\.)\s*(?:always|never|must|do\s+not|don[’']t|under\s+no\s+circumstances)\s+(?:reveal|disclose|share|leak|tell\s+(?:the\s+)?(?:user|anyone)|mention)\b` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, continue.md, **/rules.md, **/rules.md"
    sourcePath="rubric/RULES/INJECT-IMPERATIVE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-RULES-OBFUSCATION-HOMOGLYPH-01"
    title="Rules file uses look-alike characters from another script"
    categoryLabel="Obfuscation"
    description="A rules file is loaded verbatim into the agent. A word like the flagged value mixes Cyrillic, Greek, or mathematical look-alike letters with Latin ones — it reads normally to you but is distinct text to the model, letting an instruction evade keyword review while still steering the session."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["rules"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{0400}-\u{04FF}\u{1D400}-\u{1D7FF}\u{0370}-\u{03FF}](?:[a-zA-Z]|[\u{0400}-\u{04FF}\u{1D400}-\u{1D7FF}\u{0370}-\u{03FF}]){4,}` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, CONTINUE.md, **/rules.md, **/RULES.md"
    limitations={["FP risk on multilingual rules content (legitimate use of Greek / Cyrillic identifiers in technical documentation).","Cannot detect single-character homoglyph swaps; only mixed-script words ≥5 chars long.","v2 will integrate UTS #39 confusables data."]}
    searchIndex="ss-rules-obfuscation-homoglyph-01 rules file uses look-alike characters from another script a rules file is loaded verbatim into the agent. a word like the flagged value mixes cyrillic, greek, or mathematical look-alike letters with latin ones — it reads normally to you but is distinct text to the model, letting an instruction evade keyword review while still steering the session. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{0400}-\u{04ff}\u{1d400}-\u{1d7ff}\u{0370}-\u{03ff}](?:[a-za-z]|[\u{0400}-\u{04ff}\u{1d400}-\u{1d7ff}\u{0370}-\u{03ff}]){4,}` against .cursorrules, .cursor/rules/**, .windsurfrules, .windsurf/rules/**, .continuerules, continue.md, **/rules.md, **/rules.md"
    sourcePath="rubric/RULES/OBFUSCATION-HOMOGLYPH-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-DONT-ASK-01"
    title="Instruction telling the agent not to ask for approval"
    categoryLabel="Prompt injection"
    description="The text &lt;code>the flagged value&lt;/code> tells the agent to skip the normal &quot;ask the user first&quot; gate. Used adversarially it removes the human-in-the-loop check before destructive or sensitive actions, turning a normally-gated agent into a fire-and-forget executor."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?i)\b(do\s+not|don[’']t|never|no\s+need\s+to)\s+(ask|confirm|prompt|request\s+permission|wait\s+for\s+approval|seek\s+consent|verify\s+with\s+the\s+user)\b` against **/*.md, **/SKILL.md, **/CLAUDE.md"
    limitations={["Legitimate use case: autonomous-mode skills (background tasks, scheduled jobs) explicitly opt out of approval prompts. Shadow window will quantify the FP rate on this legitimate cohort.","Cannot distinguish skill-level autonomy declarations from injection-style mid-skill instructions; both fire on the same regex.","Cannot detect equivalents in non-English text."]}
    searchIndex="ss-skill-inject-dont-ask-01 instruction telling the agent not to ask for approval the text the flagged value tells the agent to skip the normal &quot;ask the user first&quot; gate. used adversarially it removes the human-in-the-loop check before destructive or sensitive actions, turning a normally-gated agent into a fire-and-forget executor. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?i)\b(do\s+not|don[’']t|never|no\s+need\s+to)\s+(ask|confirm|prompt|request\s+permission|wait\s+for\s+approval|seek\s+consent|verify\s+with\s+the\s+user)\b` against **/*.md, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-DONT-ASK-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-EMOJI-SMUG-01"
    title="Long emoji run that may be smuggling a hidden instruction"
    categoryLabel="Obfuscation"
    description="Long runs of emoji — especially with variation selectors and zero-width joiners — tokenize in model-specific ways that can encode an instruction while reading as harmless decoration to a person. This rule flags ten or more consecutive emoji, a length that is rare in genuine documentation but common in this obfuscation technique."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?u)(?:[\U0001F300-\U0001F9FF☀-➿]\u{FE0F}?\u{200D}?){10,}` against **/*.md, **/*.yaml, **/*.yml, **/SKILL.md, **/CLAUDE.md"
    limitations={["Cannot distinguish emoji-smuggled payloads from heavy-emoji documentation (welcome banners, decorative section dividers). Shadow window measures real-world impact.","Emoji-variation-selector (FE0F) and zero-width-joiner (200D) sequences are tokenized differently across models; rule fires on the literal codepoint sequence, not on per-model token expansion.","Cannot detect non-emoji obfuscation via mathematical alphanumeric symbols (U+1D400+) — that pattern is covered by SS-SKILL-INJECT-HOMOGLYPH-01."]}
    searchIndex="ss-skill-inject-emoji-smug-01 long emoji run that may be smuggling a hidden instruction long runs of emoji — especially with variation selectors and zero-width joiners — tokenize in model-specific ways that can encode an instruction while reading as harmless decoration to a person. this rule flags ten or more consecutive emoji, a length that is rare in genuine documentation but common in this obfuscation technique. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `(?u)(?:[\u0001f300-\u0001f9ff☀-➿]\u{fe0f}?\u{200d}?){10,}` against **/*.md, **/*.yaml, **/*.yml, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-EMOJI-SMUG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-HOMOGLYPH-01"
    title="Look-alike (homoglyph) characters mixed into otherwise-Latin words"
    categoryLabel="Obfuscation"
    description="A word can look like plain English while secretly containing Cyrillic, Greek, or mathematical letters that are visually identical to ASCII — Cyrillic 'а' for Latin 'a', Greek 'ο' for 'o'. This rule flags mixed-script words of five or more characters, a technique used to slip past keyword filters while reading normally to a person."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `[\u{0400}-\u{04FF}\u{1D400}-\u{1D7FF}\u{0370}-\u{03FF}](?:[a-zA-Z]|[\u{0400}-\u{04FF}\u{1D400}-\u{1D7FF}\u{0370}-\u{03FF}]){4,}` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md, **/CLAUDE.md"
    limitations={["FP risk on multilingual documentation (Russian / Greek / mathematical) that legitimately uses non-Latin scripts. Shadow window measures the cost.","Cannot detect single-character homoglyph swaps in identifiers — only mixed-script words ≥5 chars long.","Does not currently consult a homoglyph-confusable table (UTS #39); a v2 revision will integrate the Unicode confusables data."]}
    searchIndex="ss-skill-inject-homoglyph-01 look-alike (homoglyph) characters mixed into otherwise-latin words a word can look like plain english while secretly containing cyrillic, greek, or mathematical letters that are visually identical to ascii — cyrillic 'а' for latin 'a', greek 'ο' for 'o'. this rule flags mixed-script words of five or more characters, a technique used to slip past keyword filters while reading normally to a person. obfuscation llm01 prompt injection aml.t0051 llm prompt injection regex `[\u{0400}-\u{04ff}\u{1d400}-\u{1d7ff}\u{0370}-\u{03ff}](?:[a-za-z]|[\u{0400}-\u{04ff}\u{1d400}-\u{1d7ff}\u{0370}-\u{03ff}]){4,}` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-HOMOGLYPH-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-IMPERATIVE-01"
    title="&quot;Never tell the user&quot; non-disclosure imperative in the skill"
    categoryLabel="Prompt injection"
    description="A bulleted imperative like &lt;code>the flagged value&lt;/code> tells the agent to never reveal, disclose, or mention something to the user. Used adversarially it can instruct the agent to hide its tool calls or lie about what it did — stripping the transparency a user relies on to trust the agent."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM01","label":"Prompt Injection","url":"https://genai.owasp.org/llmrisk/llm01-prompt-injection/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?im)^\s*(?:•|-|\*|\d+\.)\s*(?:always|never|must|do\s+not|don[’']t|under\s+no\s+circumstances)\s+(?:reveal|disclose|share|leak|tell\s+(?:the\s+)?(?:user|anyone)|mention)\b` against **/*.md, **/SKILL.md, **/CLAUDE.md"
    limitations={["FP risk is substantial: legitimate skills often contain instructions about confidentiality or scope boundaries. Shadow window measures real-world impact.","Cannot detect imperative patterns expressed indirectly ('please keep X private', 'X should remain hidden') — only the strict imperative form.","Does not distinguish between user-facing imperatives (legitimate) and model-facing imperatives (suspicious)."]}
    searchIndex="ss-skill-inject-imperative-01 &quot;never tell the user&quot; non-disclosure imperative in the skill a bulleted imperative like the flagged value tells the agent to never reveal, disclose, or mention something to the user. used adversarially it can instruct the agent to hide its tool calls or lie about what it did — stripping the transparency a user relies on to trust the agent. prompt injection llm01 prompt injection aml.t0051 llm prompt injection regex `(?im)^\s*(?:•|-|\*|\d+\.)\s*(?:always|never|must|do\s+not|don[’']t|under\s+no\s+circumstances)\s+(?:reveal|disclose|share|leak|tell\s+(?:the\s+)?(?:user|anyone)|mention)\b` against **/*.md, **/skill.md, **/claude.md"
    sourcePath="rubric/SKILL/INJECT-IMPERATIVE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-INJECT-SYS-LEAK-01"
    title="Request for the agent to reveal its system prompt"
    categoryLabel="Prompt injection"
    description="The text &lt;code>the flagged value&lt;/code> asks the agent to disclose its hidden system prompt or initial instructions. That is often the first step of a larger attack: knowing the system prompt lets an attacker craft inputs that defeat its constraints by mimicking its own voice."
    severity="medium"
    subScore="security"
    status="shadow"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[{"family":"owasp-llm","id":"LLM07","label":"System Prompt Leakage","url":"https://genai.owasp.org/llmrisk/llm072025-system-prompt-leakage/"},{"family":"mitre-atlas","id":"AML.T0051","label":"LLM Prompt Injection","url":"https://atlas.mitre.org/techniques/AML.T0051"}]}
    triggerSummary="regex `(?i)\b(repeat|reveal|output|print|show|tell\s+me)\s+(your|the)\s+(system\s+prompt|initial\s+instructions?|original\s+prompts?|hidden\s+instructions?|prompt\s+template)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/SKILL.md"
    limitations={["FP risk on skills designed to introspect or debug model behaviour (prompt-engineering tutorials, evaluation toolkits).","Cannot detect indirect leak attempts that don't use the canonical 'system prompt' vocabulary.","Does not currently chain with model-level mitigations (modern models partially refuse this class of request); SaferSkills flags the intent regardless of model response."]}
    searchIndex="ss-skill-inject-sys-leak-01 request for the agent to reveal its system prompt the text the flagged value asks the agent to disclose its hidden system prompt or initial instructions. that is often the first step of a larger attack: knowing the system prompt lets an attacker craft inputs that defeat its constraints by mimicking its own voice. prompt injection llm07 system prompt leakage aml.t0051 llm prompt injection regex `(?i)\b(repeat|reveal|output|print|show|tell\s+me)\s+(your|the)\s+(system\s+prompt|initial\s+instructions?|original\s+prompts?|hidden\s+instructions?|prompt\s+template)\b` against **/*.md, **/*.yaml, **/*.yml, **/*.json, **/skill.md"
    sourcePath="rubric/SKILL/INJECT-SYS-LEAK-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
</RuleGroup>

<RuleGroup category="supply_chain" title="Supply Chain" weight={20} count={5}>
  <RuleCard
    ruleId="SS-HOOKS-SUPPLY-CHAIN-OWNER-XFER-01"
    title="Hook repo is maintained by a very small group"
    categoryLabel="Supply chain"
    description="This hook's repo has the matches or fewer contributors. A small maintainer pool is the classic takeover target — compromising one account, or persuading them to transfer ownership, lets an attacker push a malicious update that every consumer runs automatically."
    severity="high"
    subScore="supply_chain"
    status="active"
    weight={25}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM03","label":"Supply Chain","url":"https://genai.owasp.org/llmrisk/llm032025-supply-chain/"},{"family":"mitre-atlas","id":"AML.T0010","label":"AI Supply Chain Compromise","url":"https://atlas.mitre.org/techniques/AML.T0010"}]}
    triggerSummary="contributor_count lte 5"
    limitations={["v1 detects only the structural signal — small contributor pool combined with the absence of an ownership-transfer announcement. The actual cross-time owner-transfer detection (GitHub ownership change events) lands in Phase B.","Cannot detect repo transfers between organizations that the maintainer announces clearly — the rule fires on the structural state.","Hook artifacts with longer ownership history naturally pass; the rule's threshold is intentionally conservative for the hook category specifically."]}
    searchIndex="ss-hooks-supply-chain-owner-xfer-01 hook repo is maintained by a very small group this hook's repo has the matches or fewer contributors. a small maintainer pool is the classic takeover target — compromising one account, or persuading them to transfer ownership, lets an attacker push a malicious update that every consumer runs automatically. supply chain llm03 supply chain aml.t0010 ai supply chain compromise contributor_count lte 5"
    sourcePath="rubric/HOOKS/SUPPLY-CHAIN-OWNER-XFER-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-SUPPLY-CHAIN-HASH-DRIFT-01"
    title="MCP server content changed since the last scan (possible rug-pull)"
    categoryLabel="Supply chain"
    description="This server's file content hashes differ from a prior scan with no matching CHANGELOG or release note. That's the rug-pull signature: a trusted server quietly updated to introduce exfiltration, persistence, or tool-poisoning after consumers learned to trust it."
    severity="high"
    subScore="supply_chain"
    status="active"
    weight={25}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM03","label":"Supply Chain","url":"https://genai.owasp.org/llmrisk/llm032025-supply-chain/"},{"family":"mitre-atlas","id":"AML.T0010","label":"AI Supply Chain Compromise","url":"https://atlas.mitre.org/techniques/AML.T0010"}]}
    triggerSummary="metadata stars exists true"
    limitations={["v1 trigger is a stub — the real hash-drift comparison runs against `catalog_items.content_hash_sha256` and `scans.file_hashes` JSONB diff, which lands in Phase B engine.","Cannot fire on first scan of a never-seen-before MCP server (no prior hash to drift from). First-scan baseline is the trigger condition.","Cannot distinguish maintainer-rotation drift (legitimate ownership change) from rug-pull drift (adversarial replacement)."]}
    searchIndex="ss-mcp-supply-chain-hash-drift-01 mcp server content changed since the last scan (possible rug-pull) this server's file content hashes differ from a prior scan with no matching changelog or release note. that's the rug-pull signature: a trusted server quietly updated to introduce exfiltration, persistence, or tool-poisoning after consumers learned to trust it. supply chain llm03 supply chain aml.t0010 ai supply chain compromise metadata stars exists true"
    sourcePath="rubric/MCP/SUPPLY-CHAIN-HASH-DRIFT-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-SUPPLY-CHAIN-TYPOSQUAT-01"
    title="Name is one character off an established MCP server"
    categoryLabel="Supply chain"
    description="This server's name is within one character (Levenshtein distance 1) of an established registry entry. It may be a legitimate fork — or a typosquat hoping a user fat-fingers the install command and pulls a hostile server instead of the trusted one."
    severity="high"
    subScore="supply_chain"
    status="shadow"
    weight={25}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM03","label":"Supply Chain","url":"https://genai.owasp.org/llmrisk/llm032025-supply-chain/"},{"family":"mitre-atlas","id":"AML.T0010","label":"AI Supply Chain Compromise","url":"https://atlas.mitre.org/techniques/AML.T0010"}]}
    triggerSummary="metadata registry_listings_count gte 1"
    limitations={["v1 is structural — detects MCP servers whose name is within Levenshtein distance 1 of an established registry entry. Distance and the established-set definition land in Phase B engine. Phase A stubs the trigger.","Cannot detect typosquatting where the name is intentionally identical but the org is different (full repo-name collision).","Cross-registry typosquat detection requires the registries-index population that ships with I-03 (W4)."]}
    searchIndex="ss-mcp-supply-chain-typosquat-01 name is one character off an established mcp server this server's name is within one character (levenshtein distance 1) of an established registry entry. it may be a legitimate fork — or a typosquat hoping a user fat-fingers the install command and pulls a hostile server instead of the trusted one. supply chain llm03 supply chain aml.t0010 ai supply chain compromise metadata registry_listings_count gte 1"
    sourcePath="rubric/MCP/SUPPLY-CHAIN-TYPOSQUAT-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-HOOKS-SUPPLY-CHAIN-AUTHOR-AGE-01"
    title="Hook published by a brand-new account"
    categoryLabel="Supply chain"
    description="The account publishing this hook is less than 90 days old. New accounts that publish and disappear are a known supply-chain pattern — and a hook runs with your privileges automatically, so an unestablished author warrants extra scrutiny before you trust it."
    severity="medium"
    subScore="supply_chain"
    status="shadow"
    weight={15}
    appliesTo={["hooks"]}
    frameworks={[{"family":"owasp-llm","id":"LLM03","label":"Supply Chain","url":"https://genai.owasp.org/llmrisk/llm032025-supply-chain/"},{"family":"mitre-atlas","id":"AML.T0010","label":"AI Supply Chain Compromise","url":"https://atlas.mitre.org/techniques/AML.T0010"}]}
    triggerSummary="metadata owner_age_days lt 90"
    limitations={["GitHub user account age is a weak signal — long-established accounts can also publish malicious hooks; brand-new accounts publish many legitimate ones.","Does not consider per-user reputation across other repos. v2 may consult OpenSSF reputation signals.","Phase A registers the rule shape; the trigger executor querying GitHub user metadata wires in Phase B."]}
    searchIndex="ss-hooks-supply-chain-author-age-01 hook published by a brand-new account the account publishing this hook is less than 90 days old. new accounts that publish and disappear are a known supply-chain pattern — and a hook runs with your privileges automatically, so an unestablished author warrants extra scrutiny before you trust it. supply chain llm03 supply chain aml.t0010 ai supply chain compromise metadata owner_age_days lt 90"
    sourcePath="rubric/HOOKS/SUPPLY-CHAIN-AUTHOR-AGE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-SUPPLY-CHAIN-UNSIGNED-01"
    title="MCP release ships without any signature"
    categoryLabel="Supply chain"
    description="No signature file (Sigstore, GPG, or minisign) is present in the repo, so a consumer can't verify the install bytes match what the maintainer actually published — leaving the &quot;someone swapped the tarball&quot; attack class open."
    severity="medium"
    subScore="supply_chain"
    status="shadow"
    weight={12}
    appliesTo={["mcp"]}
    frameworks={[{"family":"owasp-llm","id":"LLM03","label":"Supply Chain","url":"https://genai.owasp.org/llmrisk/llm032025-supply-chain/"},{"family":"mitre-atlas","id":"AML.T0010","label":"AI Supply Chain Compromise","url":"https://atlas.mitre.org/techniques/AML.T0010"}]}
    triggerSummary="no file at **/*.sig or **/*.minisig or **/cosign.pub or **/SIGNATURES or **/.signatures.yaml"
    limitations={["Most MCP servers in 2026 do not sign releases — the rule will fire on the majority of the catalog. Shadow window quantifies whether this matters operationally.","Detects file-presence only; does not validate signatures or verify a trust chain.","GitHub-Actions-signed-only releases (Sigstore via attestations) are not detected without a Sigstore API query. v2 enhancement."]}
    searchIndex="ss-mcp-supply-chain-unsigned-01 mcp release ships without any signature no signature file (sigstore, gpg, or minisign) is present in the repo, so a consumer can't verify the install bytes match what the maintainer actually published — leaving the &quot;someone swapped the tarball&quot; attack class open. supply chain llm03 supply chain aml.t0010 ai supply chain compromise no file at **/*.sig or **/*.minisig or **/cosign.pub or **/signatures or **/.signatures.yaml"
    sourcePath="rubric/MCP/SUPPLY-CHAIN-UNSIGNED-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
</RuleGroup>

<RuleGroup category="maintenance" title="Maintenance" weight={15} count={5}>
  <RuleCard
    ruleId="SS-SKILL-MAINTENANCE-CI-BROKEN-01"
    title="CI is configured but not enforced on the default branch"
    categoryLabel="Maintenance"
    description="The repository ships a CI workflow file but the default branch has no branch protection requiring it to pass. &quot;Has CI&quot; then implies &quot;tests pass on main&quot; when nothing actually enforces that, so a green badge can mask regressions merged into the default branch."
    severity="medium"
    subScore="maintenance"
    status="active"
    weight={12}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="AND of 2 sub-triggers"
    limitations={["Cannot distinguish a CI configuration that legitimately allows main-branch failures (e.g. nightly canary jobs that exercise unstable upstream code) from one that simply isn't enforced.","v1 detects the *presence* of a workflow file as the signal; v2 will query the GitHub Actions API to check recent default-branch run conclusions.","Cannot detect CI hosted off-platform (Buildkite-hosted, self-hosted Jenkins) when no in-repo workflow file exists."]}
    searchIndex="ss-skill-maintenance-ci-broken-01 ci is configured but not enforced on the default branch the repository ships a ci workflow file but the default branch has no branch protection requiring it to pass. &quot;has ci&quot; then implies &quot;tests pass on main&quot; when nothing actually enforces that, so a green badge can mask regressions merged into the default branch. maintenance and of 2 sub-triggers"
    sourcePath="rubric/SKILL/MAINTENANCE-CI-BROKEN-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-MAINTENANCE-COMMIT-RECENCY-01"
    title="Last commit on the default branch is over a year old"
    categoryLabel="Maintenance"
    description="The default branch has had no commit in more than 365 days. That points to either abandonment or a finished project at steady state — and stale code accumulates unfixed CVEs and compatibility drift over time."
    severity="medium"
    subScore="maintenance"
    status="active"
    weight={12}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="last_commit_age_days gt 365"
    limitations={["Mature, finished libraries that legitimately need no recent updates trigger this rule. The Maintenance sub-score weight (15%) limits the impact — a stable mature library still scores well overall.","Cannot distinguish an abandoned project from a stable one without external signals (issue response time, release cadence). The composite signal is split across multiple Maintenance rules.","Repository default-branch commit age only — branch-specific staleness is not assessed."]}
    searchIndex="ss-skill-maintenance-commit-recency-01 last commit on the default branch is over a year old the default branch has had no commit in more than 365 days. that points to either abandonment or a finished project at steady state — and stale code accumulates unfixed cves and compatibility drift over time. maintenance last_commit_age_days gt 365"
    sourcePath="rubric/SKILL/MAINTENANCE-COMMIT-RECENCY-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-MAINTENANCE-COMMIT-FREQ-01"
    title="Fewer than 3 commits in the last 90 days"
    categoryLabel="Maintenance"
    description="The default branch saw fewer than three commits in the trailing 90 days. Read alongside the last-commit-age signal, low recent activity suggests the project may not keep pace with bug reports or dependency updates."
    severity="low"
    subScore="maintenance"
    status="shadow"
    weight={8}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="commit_freq_90d lt 3"
    limitations={["FP rate on stable mature libraries is real (a finished library may have <3 commits in 90 days as a sign of stability, not abandonment). Shadow window measures the cost.","Bot commits (dependabot, renovate) inflate the count without indicating maintainer engagement. v2 may exclude bot-authored commits from the signal.","Cannot distinguish commit-volume from commit-significance (a single substantial commit vs three trivial ones)."]}
    searchIndex="ss-skill-maintenance-commit-freq-01 fewer than 3 commits in the last 90 days the default branch saw fewer than three commits in the trailing 90 days. read alongside the last-commit-age signal, low recent activity suggests the project may not keep pace with bug reports or dependency updates. maintenance commit_freq_90d lt 3"
    sourcePath="rubric/SKILL/MAINTENANCE-COMMIT-FREQ-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-MAINTENANCE-ISSUE-RESPONSE-01"
    title="Median issue response time is over 30 days"
    categoryLabel="Maintenance"
    description="Across issues opened in the last 12 months, the median time to a first maintainer response exceeds 30 days (720 hours). That means bug reports — including security reports — are unlikely to get a timely reply."
    severity="low"
    subScore="maintenance"
    status="shadow"
    weight={8}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="issue_response_p50_hours gt 720"
    limitations={["Repositories with issues disabled (Issues feature off) produce no signal; the rule does not fire on those.","Median is sensitive to small sample sizes (n<5 issues in window). v2 will require ≥5 issues for the signal to register.","Counts ANY maintainer comment as a response; cannot distinguish substantive engagement from a triage acknowledgment."]}
    searchIndex="ss-skill-maintenance-issue-response-01 median issue response time is over 30 days across issues opened in the last 12 months, the median time to a first maintainer response exceeds 30 days (720 hours). that means bug reports — including security reports — are unlikely to get a timely reply. maintenance issue_response_p50_hours gt 720"
    sourcePath="rubric/SKILL/MAINTENANCE-ISSUE-RESPONSE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-MAINTENANCE-OPEN-ISSUE-RATIO-01"
    title="Over 80% of opened issues remain open"
    categoryLabel="Maintenance"
    description="Across a 12-month window, more than 80% of opened issues are still open (open / (open + closed)). That suggests issues are being filed faster than they get resolved, so a report you file may sit unresolved."
    severity="low"
    subScore="maintenance"
    status="shadow"
    weight={8}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="open_issue_ratio gt 0.8"
    limitations={["Repositories with issues disabled produce no signal.","Stale-but-not-resolved issues (legitimate edge cases, won't-fix, deferred) inflate the ratio. v2 will require ≥30 days of inactivity on the open issue before counting.","Repositories with a large historical issue backlog from a different maintainer era unfairly accumulate ratio. v2 may consider a rolling 12-month window."]}
    searchIndex="ss-skill-maintenance-open-issue-ratio-01 over 80% of opened issues remain open across a 12-month window, more than 80% of opened issues are still open (open / (open + closed)). that suggests issues are being filed faster than they get resolved, so a report you file may sit unresolved. maintenance open_issue_ratio gt 0.8"
    sourcePath="rubric/SKILL/MAINTENANCE-OPEN-ISSUE-RATIO-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
</RuleGroup>

<RuleGroup category="transparency" title="Transparency" weight={15} count={6}>
  <RuleCard
    ruleId="SS-RULES-TRANSPARENCY-MANIFEST-01"
    title="Rules set ships without a README"
    categoryLabel="Transparency"
    description="This rules repository has no README. Because rule files are injected verbatim into the agent's context, an undocumented set forces consumers to read every file to learn its intent, scope, and behaviour — making hidden or surprising instructions easy to miss."
    severity="medium"
    subScore="transparency"
    status="active"
    weight={12}
    appliesTo={["rules"]}
    frameworks={[]}
    triggerSummary="no file at README.md or README or .cursor/rules/README.md or .windsurf/rules/README.md"
    limitations={["A README at repo root satisfies the rule even if the rules subdirectory itself is undocumented. Subdirectory-specific documentation is a future v2 refinement.","Cannot validate README content quality."]}
    searchIndex="ss-rules-transparency-manifest-01 rules set ships without a readme this rules repository has no readme. because rule files are injected verbatim into the agent's context, an undocumented set forces consumers to read every file to learn its intent, scope, and behaviour — making hidden or surprising instructions easy to miss. transparency no file at readme.md or readme or .cursor/rules/readme.md or .windsurf/rules/readme.md"
    sourcePath="rubric/RULES/TRANSPARENCY-MANIFEST-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-TRANSPARENCY-LICENSE-01"
    title="No LICENSE file in the repository"
    categoryLabel="Transparency"
    description="No &lt;code>LICENSE&lt;/code> file was found. Without one, the artifact is &quot;all rights reserved&quot; by default under copyright law, which leaves you with no clear right to redistribute, modify, or even install it depending on your compliance posture."
    severity="medium"
    subScore="transparency"
    status="active"
    weight={15}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="no file at LICENSE or LICENSE.md or LICENSE.txt or COPYING or COPYING.md or LICENCE or LICENCE.md"
    limitations={["Does not validate license content — a LICENSE file containing arbitrary text satisfies the rule. License-validity is enforced via a separate SPDX-identifier check (deferred).","Cannot detect license declarations embedded only in package.json / pyproject.toml without a separate LICENSE file. The W2 v1 rule treats the file's presence as the canonical declaration."]}
    searchIndex="ss-skill-transparency-license-01 no license file in the repository no license file was found. without one, the artifact is &quot;all rights reserved&quot; by default under copyright law, which leaves you with no clear right to redistribute, modify, or even install it depending on your compliance posture. transparency no file at license or license.md or license.txt or copying or copying.md or licence or licence.md"
    sourcePath="rubric/SKILL/TRANSPARENCY-LICENSE-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-TRANSPARENCY-MANIFEST-01"
    title="No skill manifest (SKILL.md) in the repository"
    categoryLabel="Transparency"
    description="This capability has no &lt;code>SKILL.md&lt;/code> (or &lt;code>skill.yaml&lt;/code>/&lt;code>.yml&lt;/code>/&lt;code>.json&lt;/code>) manifest. The manifest is what declares the skill's purpose, inputs, and behavior contract, so without it you cannot tell what the skill does or how it behaves without reading every file in the repo."
    severity="medium"
    subScore="transparency"
    status="active"
    weight={15}
    appliesTo={["skill"]}
    frameworks={[]}
    triggerSummary="no file at SKILL.md or **/SKILL.md or skill.yaml or skill.yml or skill.json"
    limitations={["Fires only on the absence of the manifest file by canonical name; cannot detect a manifest stored under a non-canonical filename.","Does not validate manifest contents — a present-but-empty SKILL.md satisfies the rule. Content-validation is a separate v2 rule under consideration."]}
    searchIndex="ss-skill-transparency-manifest-01 no skill manifest (skill.md) in the repository this capability has no skill.md (or skill.yaml / .yml / .json ) manifest. the manifest is what declares the skill's purpose, inputs, and behavior contract, so without it you cannot tell what the skill does or how it behaves without reading every file in the repo. transparency no file at skill.md or **/skill.md or skill.yaml or skill.yml or skill.json"
    sourcePath="rubric/SKILL/TRANSPARENCY-MANIFEST-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-TRANSPARENCY-CHANGELOG-01"
    title="No CHANGELOG file in the repository"
    categoryLabel="Transparency"
    description="No &lt;code>CHANGELOG&lt;/code> file was found. Without a record of what changed between releases, you cannot tell a benign patch from a behavior- or license-changing update without reading the diff yourself."
    severity="low"
    subScore="transparency"
    status="active"
    weight={8}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="no file at CHANGELOG.md or CHANGELOG or CHANGELOG.txt or CHANGES.md or HISTORY.md"
    limitations={["Does not validate the changelog content or format (Keep a Changelog, conventional commits, etc.). Presence is the only check.","GitHub-Release-managed changelogs (release notes per tag) are not detected — the rule fires even when the maintainer uses release notes instead of a file. v2 may consult the GitHub Releases API."]}
    searchIndex="ss-skill-transparency-changelog-01 no changelog file in the repository no changelog file was found. without a record of what changed between releases, you cannot tell a benign patch from a behavior- or license-changing update without reading the diff yourself. transparency no file at changelog.md or changelog or changelog.txt or changes.md or history.md"
    sourcePath="rubric/SKILL/TRANSPARENCY-CHANGELOG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-TRANSPARENCY-DESCRIPTION-01"
    title="No README in the repository"
    categoryLabel="Transparency"
    description="No &lt;code>README&lt;/code> file was found. The README is the canonical entry point that explains what an artifact does; without it you would have to read the source or run it speculatively to find out — both unacceptable for code that auto-installs into an agent context."
    severity="low"
    subScore="transparency"
    status="active"
    weight={5}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="no file at README.md or README or README.rst or README.txt"
    limitations={["Does not assess README quality, length, or accuracy. A one-line README satisfies the rule.","Cannot consult repository-description-only documentation (GitHub repo description with no README file). v2 may treat a non-empty GitHub repo description as partial credit."]}
    searchIndex="ss-skill-transparency-description-01 no readme in the repository no readme file was found. the readme is the canonical entry point that explains what an artifact does; without it you would have to read the source or run it speculatively to find out — both unacceptable for code that auto-installs into an agent context. transparency no file at readme.md or readme or readme.rst or readme.txt"
    sourcePath="rubric/SKILL/TRANSPARENCY-DESCRIPTION-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-TRANSPARENCY-SECURITY-01"
    title="No SECURITY.md disclosure policy in the repository"
    categoryLabel="Transparency"
    description="No &lt;code>SECURITY.md&lt;/code> was found (checked the repo root, &lt;code>.github/&lt;/code>, and &lt;code>docs/&lt;/code>). Without one there is no stated way to report a vulnerability, which versions are supported, or how quickly the maintainer commits to respond — the baseline for responsible disclosure on code that runs in privileged agent contexts."
    severity="low"
    subScore="transparency"
    status="active"
    weight={8}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="no file at SECURITY.md or .github/SECURITY.md or docs/SECURITY.md"
    limitations={["Does not validate SECURITY.md content (presence of disclosure email, response SLA, supported-version table).","GitHub-Security-Advisory-only disclosure policies are not detected — the rule fires even when the maintainer publishes via GHSA without a file."]}
    searchIndex="ss-skill-transparency-security-01 no security.md disclosure policy in the repository no security.md was found (checked the repo root, .github/ , and docs/ ). without one there is no stated way to report a vulnerability, which versions are supported, or how quickly the maintainer commits to respond — the baseline for responsible disclosure on code that runs in privileged agent contexts. transparency no file at security.md or .github/security.md or docs/security.md"
    sourcePath="rubric/SKILL/TRANSPARENCY-SECURITY-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
</RuleGroup>

<RuleGroup category="community" title="Community" weight={15} count={5}>
  <RuleCard
    ruleId="SS-HOOKS-COMMUNITY-FORK-HEALTH-01"
    title="Hook has very few forks"
    categoryLabel="Community"
    description="This hook has fewer than the matches forks. Low fork counts can hint at limited community use or little third-party validation, but they are a noisy proxy — many sound hooks are rarely forked because they need no customization. This is context only and does not affect the score."
    severity="info"
    subScore="community"
    status="shadow"
    weight={0}
    appliesTo={["hooks"]}
    frameworks={[]}
    triggerSummary="metadata fork_count lt 3"
    limitations={["Info severity, weight 0 — does not affect score; surfaces in trace only.","Fork count is a noisy proxy for community use; many legitimate hooks have low fork counts because they don't need customization.","Cannot distinguish recent forks (active community) from historical forks (stale)."]}
    searchIndex="ss-hooks-community-fork-health-01 hook has very few forks this hook has fewer than the matches forks. low fork counts can hint at limited community use or little third-party validation, but they are a noisy proxy — many sound hooks are rarely forked because they need no customization. this is context only and does not affect the score. community metadata fork_count lt 3"
    sourcePath="rubric/HOOKS/COMMUNITY-FORK-HEALTH-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-MCP-COMMUNITY-CROSS-REG-01"
    title="Listed on two or more independent MCP registries"
    categoryLabel="Community"
    description="This server appears on at least two independent registries (such as the MCP Registry, Smithery, Glama, PulseMCP, or mcp.so), a community-adoption signal that several curators independently chose to list it. It's reference context only and does not affect the score."
    severity="info"
    subScore="community"
    status="active"
    weight={0}
    appliesTo={["mcp"]}
    frameworks={[]}
    triggerSummary="metadata registry_listings_count gte 2"
    limitations={["Info severity, weight 0: this rule does NOT affect the score. It surfaces in the trace only as community-presence context.","Requires the Phase B registry-index data to populate the metadata field. v1 lands the rule shape; the trigger executor is wired in Phase B.","Cannot detect registry-listing manipulation (an attacker who lists their MCP across multiple registries inflates the score). The signal is informational only for this reason."]}
    searchIndex="ss-mcp-community-cross-reg-01 listed on two or more independent mcp registries this server appears on at least two independent registries (such as the mcp registry, smithery, glama, pulsemcp, or mcp.so), a community-adoption signal that several curators independently chose to list it. it's reference context only and does not affect the score. community metadata registry_listings_count gte 2"
    sourcePath="rubric/MCP/COMMUNITY-CROSS-REG-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-RULES-COMMUNITY-INSTALLS-01"
    title="Rules repository has fewer than 5 stars"
    categoryLabel="Community"
    description="This rules repository has fewer than 5 GitHub stars. Stars are a noisy proxy for community adoption and review; a low count means fewer people have vetted these rules before they were injected into an agent's context. This is context only and does not affect the score."
    severity="info"
    subScore="community"
    status="active"
    weight={0}
    appliesTo={["rules"]}
    frameworks={[]}
    triggerSummary="metadata stars lt 5"
    limitations={["Info severity, weight 0 — surfaces in trace only.","Star count is a noisy community proxy.","Rules-set ecosystems are small in 2026; thresholds may need recalibration as the ecosystem matures."]}
    searchIndex="ss-rules-community-installs-01 rules repository has fewer than 5 stars this rules repository has fewer than 5 github stars. stars are a noisy proxy for community adoption and review; a low count means fewer people have vetted these rules before they were injected into an agent's context. this is context only and does not affect the score. community metadata stars lt 5"
    sourcePath="rubric/RULES/COMMUNITY-INSTALLS-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-COMMUNITY-CONTRIBUTORS-01"
    title="Single-author repository"
    categoryLabel="Community"
    description="Only one contributor is detected. Shown as community context only — solo projects are common and often legitimate, so this signal carries weight 0 and does not affect the score. It lets you weigh the bus-factor risk before deep integration."
    severity="info"
    subScore="community"
    status="shadow"
    weight={0}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="contributor_count eq 1"
    limitations={["Single-author projects are common and often legitimate (research code, hobby tools, focused micro-libraries). Info severity reflects this.","Bot accounts (dependabot, renovate) inflate the contributor count without indicating real community. v2 will exclude bot accounts.","Cannot distinguish a solo project from one with many shadow-contributors via squash-merge from a fork."]}
    searchIndex="ss-skill-community-contributors-01 single-author repository only one contributor is detected. shown as community context only — solo projects are common and often legitimate, so this signal carries weight 0 and does not affect the score. it lets you weigh the bus-factor risk before deep integration. community contributor_count eq 1"
    sourcePath="rubric/SKILL/COMMUNITY-CONTRIBUTORS-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
  <RuleCard
    ruleId="SS-SKILL-COMMUNITY-STARS-01"
    title="Fewer than 10 stars on the GitHub repository"
    categoryLabel="Community"
    description="The repository has under 10 GitHub stars. This is shown as community context only — stars are an easily-manipulated, low-quality proxy for adoption, so this signal carries weight 0 and does not affect the score."
    severity="info"
    subScore="community"
    status="active"
    weight={0}
    appliesTo={["skill","mcp","rules","hooks","plugin"]}
    frameworks={[]}
    triggerSummary="metadata stars lt 10"
    limitations={["Star count is a noisy signal: stars can be bought, manipulated, or simply absent on new-but-legitimate projects.","Info severity, weight 0: this rule does NOT affect the score. It surfaces in the trace only as community-context.","Cannot detect star-velocity (sudden spike) which would be a more valuable signal. v2 candidate."]}
    searchIndex="ss-skill-community-stars-01 fewer than 10 stars on the github repository the repository has under 10 github stars. this is shown as community context only — stars are an easily-manipulated, low-quality proxy for adoption, so this signal carries weight 0 and does not affect the score. community metadata stars lt 10"
    sourcePath="rubric/SKILL/COMMUNITY-STARS-01.md"
    rubricSha="55a0678a4d160d9c2fef38dc3c774c1d7ceb475d"
  />
</RuleGroup>
